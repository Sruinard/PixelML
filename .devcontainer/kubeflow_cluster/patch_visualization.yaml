spec:
  template:
    spec:
      containers:
      - name: ml-pipeline-ui
        volumeMounts:
          - mountPath: /tmp/tfx
            name: output-volume-mount-name            
      volumes:
        - name: output-volume-mount-name
          persistentVolumeClaim:
                claimName: tfx-pvc